export interface Template {
  id: string;
  name: string;
  description: string;
  content: string;
  category: string;
  icon: string;
}

export interface TemplateCategory {
  id: string;
  name: string;
  icon: string;
}
